import Flutter
import Foundation

/// Skeleton plugin stub proving the real Flutter engine binding compiles.
public struct PluginStub {
    public let engineVersion: String

    public init(engineVersion: String = "real-engine") {
        self.engineVersion = engineVersion
    }
}
